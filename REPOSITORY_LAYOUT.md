# 仓库边界与可移植目录布局

## 主仓库定位

GitHub：<https://github.com/sunrise690/math-modeling-expert-agent>

`math-modeling-expert-agent` 只维护一个正式产品边界：数模工作台。

工作台内部包含两类能力：仓库根目录的数模 Agent 后端负责 Provider、本地知识库、质量评分、MCP、通用脚本与 Skill；`qilintex/` 负责唯一 React 前端、协作服务、QilinTeX 编辑/编译、桌面端、移动端、部署与发布。

`qilintex/apps/client/` 是整个数模工作台的唯一前端源码，QilinTeX 是其中的论文工作区，不是独立产品。原外层重复目录 `TongGaoTeX/` 已于 2026-08-11 移入 Windows 回收站。

运行时可有多个安全隔离的后端进程，但用户只进入一个前端。工作台服务端默认通过 `http://127.0.0.1:8765` 调用数模 Agent，向同一项目中的建模和论文工作区提供建模、整题交付、论文和质检模式；浏览器不直接连接数模 Agent，也不会取得其 Provider 凭据。

## 不进入主仓库的内容

- 具体年份和题号的竞赛题目、原始附件、求解代码、结果表、图件和论文。
- 大体积本地参考资料、优秀论文语料和导入后的知识库文件。
- API Key、Provider 密钥、本机认证材料和 `.agent-data/` 运行数据。
- 第三方完整仓库，例如 `impeccable`。
- QQ 下载器、蓝屏诊断、SolidWorks 维护脚本等其他独立程序。

主仓库通过根目录 `.gitignore` 忽略 `projects/`，防止具体赛题再次混入。

## 可移植布局

仓库可以克隆或解压到任意目录。所有内置路径都从当前代码位置推导：

| 相对位置 | 内容 | 可移植规则 |
| --- | --- | --- |
| `./` | 统一数模工作台与数模 Agent 后端 | 不读取开发者机器的绝对路径 |
| `qilintex/apps/client/` | 唯一前端 | Web、桌面与移动端共享同一源码 |
| `.runtime/python/` | `pnpm setup` 创建的 Python 隔离环境 | 自动创建、Git 忽略，可随时重建 |
| `.agent-data/` | 数模 Agent 运行数据 | 仓库相对、Git 忽略；部署时可显式映射到用户数据目录 |
| `qilintex/apps/server/data/` | 协作服务数据 | 从服务模块位置推导；桌面端改用系统用户数据目录 |
| `knowledge/` | 默认本地资料库 | 自动发现；外部目录通过 `AGENT_KNOWLEDGE_ROOTS` 可选配置 |

## 维护规则

1. 通用能力进入主仓库；具体赛题进入用户自选的工作目录或独立私有仓库，不约束盘符和父目录。
2. 前端功能只修改 `qilintex/apps/client/`；QilinTeX 作为论文工作区维护，不再创建独立前端或外层同步副本。
3. 本地资料库只保存清单、哈希和索引策略，不批量提交原始资料。
4. 赛题成果若需要公开，单独检查数据许可、个人信息、竞赛规则和第三方版权。
5. 源码、运行数据、赛题与大体积资料按上述相对边界维护，不把本机绝对路径写入代码或版本化文档。
